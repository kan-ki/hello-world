{
  "name": "hello-node",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "eslint": "^8.50.0",
    "prettier": "^3.0.0"
  }
}
